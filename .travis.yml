language: generic
sudo: require
services:
  - docker

before_install:
  - docker build -t mopitz199/react-app-test -f Dockerfile.dev .

script:
  - docker run -e CI=true mopitz199/react-app-test npm run test -- --coverage